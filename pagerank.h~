// pagerank.h ... interface to pagerank
// Written by Hoya Lee August 2019

#ifndef PAGERANK_H
#define PAGERANK_H

#include <stdio.h>

#define BUFSIZE 1000

void pageRank () ;
char *normaliseWord (char *str);
char **arrayOfURLs(char *rootFileName, int nV);
int getNumberOfVertices(char *rootFileName) ;
Graph generateGraph(Graph g, char* rootFileName, char **URLs);

#endif
